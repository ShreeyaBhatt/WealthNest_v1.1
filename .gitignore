# ============================================================
# WealthNest — Root .gitignore
# This tells Git which files/folders NOT to track.
# ============================================================

# ─── Node.js ────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─── Environment Variables (NEVER commit these!) ────────────
.env
.env.local
.env.development
.env.production

# ─── Python / Django ────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
env/
.venv/
*.egg-info/
dist/
build/
*.pkl               # Pickle model files (regenerate via train_models)

# ─── Django specific ────────────────────────────────────────
django_ai/db.sqlite3
django_ai/staticfiles/
django_ai/reports/*.pdf   # Generated PDFs are temporary

# ─── React / Vite ───────────────────────────────────────────
client/dist/
client/.vite/

# ─── Uploaded Files (avatars) ────────────────────────────────
server/uploads/avatars/*
!server/uploads/avatars/.gitkeep

# ─── OS Files ───────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ─── IDE / Editor ───────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ─── Logs ───────────────────────────────────────────────────
logs/
*.log

# ─── Datasets (large files — add separately if needed) ──────
django_ai/datasets/*.csv

# Exception: this one IS committed on purpose — train_models.py reads
# it directly (see that file's docstring), so it needs to exist on a
# fresh clone without anyone having to run the scraper first. At ~330KB
# it's small enough to just check in like any other project asset.
!django_ai/datasets/scf_risk_tolerance_2007.csv
