# Empty file — makes 'commands' a Python package. Django looks inside
# every app's management/commands/ folder for files like train_models.py
# and turns each one into a "python manage.py <name>" command.
